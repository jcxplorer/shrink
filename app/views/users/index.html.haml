- content_for(:bar) do
  %h2.grid_6 Users
  .grid_6
    = link_to "Add a user", new_user_path, :class => "right"

.grid_12
  %ul
    - @users.each do |user|
      %li= link_to user.name, [:edit, user]
