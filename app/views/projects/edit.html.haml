- content_for(:bar) do
  %h2.grid_8= @project.name
  .grid_4
    = button_to "Remove this project", @project, :method => :delete, :confirm => "Are you sure you want to remove this project? There is no going back!", :class => "right"

.grid_12
  = render "form"

